Are we creating shmem even if the native rule engine is not being invoked?
    i.e. The fact that it exists in server_config

test_writeLine_config_first_3477
test_writeLine_config_last_3477


Run 1 with outright skips
Run 1 with outright skips + rearrange
Run 1 with rearrange JUST BEFORE the offending tests


---

Metalnx

diceunc/metalnx

mvn package # build
# produces a .war file (packaging/docker)
# put the .war file into tomcat
# run the tomcat server
# Dockerfile describes how to run the container
    # points to the irods deployment
    # certification information maps
    
 # run:  docker run -d --rm -p 8080:8080 -v /etc/irods-ext:/etc/irods-ext  -v /home/mcc/webdavcert:/tmp/cert --add-host irods420.irodslocal:172.16.250.101 diceunc/metalnx:latest
                                            ^-- settings?                     ^-- certification                         ^-- irods                           ^-- docker image to run


- Get iRODS on a VM
- Get metalnx on local machine


maven
tomcat
dockero


metalnx config
